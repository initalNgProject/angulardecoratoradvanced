import { SetGenderPipe } from './set-gender.pipe';

describe('SetGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new SetGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
